# 修复门禁证据

各次报告独立保存，不覆盖早期结果。gate文件列出预期、实际执行集合和合法不适用原因，
report保存存储版本、迁移及容器清理结果；链路收据只含安全调用统计，不含原文、密钥或供应商响应。

| 最终门禁 | 结果 | 覆盖 |
|---|---|---|
| offline-20260921-3 | 1397/1397通过，零意外skip/xfail | 确定性协议、范围、版本、引用、容量、截止时间、费用及失败发布 |
| store-20260921-3 | 182/182通过，零意外skip/xfail | 隔离MySQL8.4.11、Neo4j5.26.30、Qdrant1.13.6，schema0014_rag_snapshots |
| model-20260921-3 | 1/1必跑用例通过，包含A/B1两个真实消息场景 | 真实embedding/rerank/生成/核验、含PDF断行材料、消息发布、HTTP引用回查与错误版本拒绝 |

真实模型用例消息仓储为独立SQLite；MySQL事务另由store门禁真实验证。不能把这组组合覆盖
说成所有组件同时经过同一个MySQL线上请求。两个临时存储门禁均确认清理3个容器、残留0。
offline使用明确的测试替身，model才调用付费模型；通过数不混用来冒充真实模型调用数量。

offline-2/model-2是段ID协议和完整阶段看门狗完成前的历史通过记录，保留以追踪实施过程。
更早失败报告与原始pytest日志保留于本地 `.rag-evaluation/reliability-*`，不对外拷贝可能带内部信息的日志。
最后模型门禁仅有既存Starlette依赖弃用警告；不存在凭据缺失或真实用例被静默跳过。

这些门禁验证工程行为和真实小样本连通性，不替代完整开发集、人工语义评分和部署费用/延迟验收。

## 修复后完整开发集 V7

V7 使用冻结 ID `72d0b3cb8cc881ca3952f986c692eb45426c3aa3bed4effc1ac3995a81347098` 完成 80/80，
A/B1 各40，未执行0。A 正常29、失败11，B1正常31、失败9；P50/P95分别为28.28/83.31秒和
30.36/108.02秒。A运行时状态为22 answered、6 partial、1 insufficient；B1为22、8、1。
全部结果和物理调用诊断见 [V7报告](../runs/2026-09-21-reliability-dev-v7/summary.md)。

标签仍待人工审核、价格未确认、保留集未运行；正常返回不是人工正确率。V7保留20次服务/契约失败，
因此不能据此采用A或B1，也没有切换默认插件。V6及更早运行均保持不可变。
