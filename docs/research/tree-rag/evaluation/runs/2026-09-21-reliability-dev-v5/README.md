# V5 摘录定位协议诊断（已中止）

原计划为40道开发题、A/B1各一次，共80次；冻结ID为
`a923c450796f15ba1edaa96fa9f5b5efd21cb7f84d5439e5ca533fae9fb5dd1f`。
源代码、配置、题集和来源快照随本目录保存；保留集未运行。

完成9条请求记录：2次partial，7次VERIFICATION_UNAVAILABLE，均为首次核验
`response_schema / span_not_found`。其余71个计划结果缺失，不能算通过或从分母删除。
第10次请求在停止进程时可能已提交给供应商，远端完成/用量/费用未知，不能记为零。

在发现重复失败后提前停止并保留全部记录，修复后另行冻结完整开发实验，不覆盖本次输出。
本次不具备完整配对质量或稳定性结论，也不生成虚构的人工评分。

独立追加的公开题 `real-law-02/A` 诊断复现：PDF原文为“未成年人的原\n则”，模型摘录为
“未成年人的原则”。`quote-diagnostic-public.json` 保存完整差异，去掉空白后相同。
该额外诊断不属于80次计划分母；启动时与末尾评测请求存在运行重叠，本次延迟不用于部署性能验收。

最初中文小样本已证明模型可能写错字符偏移，改为精确摘录后，复杂PDF又暴露换行复制不稳。
本地拒绝无效跨度的保护有效，但协议设计仍不可靠。后续改用代码预先分配的原文段ID，
模型只选择ID，代码恢复原文跨度；不去空白、不模糊匹配，也不放宽引用支持要求。
