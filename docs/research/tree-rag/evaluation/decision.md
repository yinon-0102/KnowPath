# 当前采用决定（2026-09-21）

普通 A、树形 B1 与公共链路已实现；当前两组实验配置均未通过验收，不切换默认方案。
保留 `LEARNING_RAG_PLUGIN=legacy`，A/B1 继续通过显式配置在独立环境验证。

## 依据

| 实验 | 实际请求 | 正常返回 | 服务失败 | 能支持的结论 |
|---|---:|---:|---:|---|
| 完整开发集 dev-v3 | 80 | 30（A/B1各15） | 50 | 当前配置的服务可靠性不足 |
| 预先指定预算诊断 v4 | 10 | 4（A/B1各2） | 6 | 提高输入/输出上限后仍存在严重可靠性问题 |

dev-v3 的失败包括37次输入预算超限、12次模型响应无效、1次核验异常。
v4 的失败包括4次总期限超限、1次首次核验服务不可用、1次第二次生成结构校验失败。
v4 没有记录到输入预算超限，但定向选择已见失败题，且代码也有变化，不能据此推断总体收益或预算的独立因果效果。

正常返回包含部分回答或证据不足，不能视为回答正确。60题人工标签仍为 `pending`，
价格与四项部署门槛尚未确认，质量成功率、实际费用和最终 A/B1 胜负保持未知。
[法规分析](runs/2026-09-20-dev-v3/law-review-notes.md)还发现：必要证据已进入上下文时仍可能遗漏条件；
来源跨度覆盖率及模型核验通过不能替代语义复核。

## 下一次选择实验的前置工作

1. 人工核对题目、必要答案要点、证据和可回答性，以及当前回答中的条件遗漏等疑点。
2. 在已见开发集上继续评估结构化生成/核验的可靠性、模型与计数配置；对新配置完整运行，保留全部失败。
3. 根据实际部署容量填写 `max_p95_latency_ms`、`max_mean_cost_per_request`、
   `max_b1_latency_ratio`、`max_b1_cost_ratio`，并核对供应商价格、计费口径与请求金额上限。
4. 完成以上工作后重新冻结，再运行尚未使用的保留集，按预定规则报告质量、成本和不确定性。

不因 B1 尚未胜出而自动采用尚未验收的 A。当前主要问题涉及公共生成/核验与服务可靠性，
尚不满足进入 B2 摘要导航的触发条件；B2/B3、自动路由继续保留为条件性后续实验。

## 可复核材料

- [完整开发报告](runs/2026-09-20-dev-v3/summary.md)、[逐题证据和回答](runs/2026-09-20-dev-v3/review.md)。
- [预算诊断方案与10次结果](runs/2026-09-20-budget-probe-v4/README.md)。
- [工程实施记录](../IMPLEMENTATION-STATUS.md)、[操作手册](../OPERATIONS.md)。

真实对话错误详情的最后一轮发布修复发生在 v4 冻结之后，仅以回归测试验证，未冒充参与上述模型实验。

## V7 修复后完整开发集（2026-09-21）

V7 使用修复后的冻结配置完成 80/80 请求，A/B1 各40，未执行0，未启用隐式重试。A 正常29、失败11；
B1 正常31、失败9。A 的 P50/P95 为28.28/83.31秒，B1为30.36/108.02秒。A 有22 answered、
6 partial、1 insufficient；B1有22 answered、8 partial、1 insufficient。answered仍是运行时状态，
29/31条正常回答均待人工评分。错误包括生成schema字段、核验引用/跨度、阶段期限、输出容量和一次embedding不可用。
实际embedding、rerank、generation、verification用量均已记录，部分失败调用usage未知；价格未配置，不能计算实际费用。

V7 归档及校验见 [V7报告](runs/2026-09-21-reliability-dev-v7/summary.md)。本次运行没有遗失结果，
但完整分母中的服务失败仍使两种配置都不满足上线验收；人工标签、费用和部署门槛未完成，保留集未运行。
因此继续保留legacy默认，不采用A或B1；先完成人工标签和公共链路错误修复，再决定是否进行保留集。
历史冻结输入、代码与失败原样归档，不用当前代码覆盖历史快照。
