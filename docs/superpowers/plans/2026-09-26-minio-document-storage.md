# MinIO 文档存储实施记录

目标：新上传、解析读取和资料删除完整接入 MinIO，保留 MySQL 元数据和旧文件兼容读取。
用户已授权提交及推送，提交格式为英文类型前缀加中文说明。

## 实施步骤

- [x] 在独立工作树实现，保留主工作区现有 README、忽略规则和未跟踪资料。
- [x] 新增 materials/raw_storage.py，提供 MinIO SDK 适配、环境配置、对象键与内容校验。
- [x] 通过 0015_material_object_storage 增加桶和对象引用，兼容旧 SQL 字节。
- [x] 新上传只保存 MinIO 原字节，事务回滚重查持久引用后补偿清理。
- [x] API、graph/model worker、RAG 维护入口共享环境装配和资源关闭路径。
- [x] 删除事务保留外部清理 outbox；对象及历史版本删除确认后才完成任务。
- [x] 新增 materials/raw_migration.py 命令，默认预览、回读校验、幂等重跑，显式清除 SQL 备份。
- [x] 迁移与删除统一资料锁顺序；真实 MySQL 并发测试验证对象引用不丢失。
- [x] 配置 MinIO 服务、私有桶初始化、健康检查、持久卷及仅本机端口映射。
- [x] 因社区镜像不可用，改为固定官方源码对象构建 MinIO/mc，固定基础镜像摘要。
- [x] 更新环境模板、API、基础服务、迁移和后端使用文档。
- [x] 备份本地数据库后升级至 0015；配置本地凭据，不提交 .env。
- [x] 在 infra 容器组部署并验证健康及初始化退出码；隔离真实存储测试 3 项通过。
- [x] 只读代码复核：迁移/删除并发问题已修复，最终复核无阻断项。
- [x] 完整后端回归：2325 项通过、227 项跳过；真实 MySQL/MinIO 专项 3 项通过。

交付阶段按用户授权合并主分支、重启当前应用验收，并以英文类型前缀加中文说明提交及推送。

## 验证与限制

对象存储、仓储和迁移专项 30 项通过。另以真实 MySQL 验证旧结构升级至新头及数据保留。
完整回归首轮出现旧 CLI mock 两项失败，已按新工厂接口及连接关闭语义更新；
另有一项临时目录位于 Git 仓库内导致的根目录识别失败，最终回归改用仓库外临时目录。

SQL 与 MinIO 没有分布式原子事务；进程被强杀或补偿清理失败可能留下无引用对象，
需结合 SQL 引用及 outbox 运维核对。具体恢复要求见 py/README.md。
