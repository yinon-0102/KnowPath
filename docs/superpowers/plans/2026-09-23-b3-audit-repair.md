# B3 审查修复与公平复测

用户已批准实现。完成后交付结果分析，暂不提交或推送。

## 固定边界

- 保留原 40 题、金标准和三份原文；A/B2-R1/B3 各 40 次、交替顺序、一次重复、零评测器重试。
- 每次最多 40 叶子候选、一次查询 embedding、一次 rerank。保留已冻结的 leaf-centroid 单元向量表示。
- 原始单叶 Recall/MRR/NDCG 保留；补充跨叶子连续区间完整覆盖 Recall@1/3/5/10/20/40。
- A 基线已经具有 continuation 原子上下文装箱；报告不得再描述为独立叶子装箱。
- 原有采纳门槛不降低：最终必要证据覆盖相对同配置 A 至少 +3 个百分点；负例不退化；P95 ≤ A 的 1.2 倍；质量审查未完成时不作正式采纳结论。

## 实施步骤

1. 保存修复前源代码、原始冻结配置和结果；记录 SHA256，保留此前所有未提交改动。
2. 评分回归测试：跨叶子相邻区间可联合覆盖，间隙、不同资料版本不可拼接。B3 新增证据相对完整 A 候选池计算；缺失基线记 unknown，不能记零。增量潜力和最终候选覆盖分别报告。
3. B3 回归测试与修复：任意成员 seed + unit hit 可准入；只为新增成员消耗候选名额；rerank 文本与实际成员一致；结构失败回退真实 A 排序；embedding/deadline 错误传播；unit/leaf 版本与源映射一致；pipeline 从可信原文重建组并校验 reranker 返回身份。
4. 使用真实 AnswerCapacity 离线重放旧排序：先在 12,000 下复现 120 条原上下文，再统一比较 14,000 输入预算。总预算 16,000、输出 2,000、草稿上限 4,000 字节、双重序列化预留和保守计量不变。14,000 来自已有应用预算余量，并非对供应商真实窗口的断言。
5. 独立规格审查、代码质量审查和相应测试。新建隔离评测目录/SQLite 副本；验证 970 叶子、934 unit 的规范身份与 Qdrant 记录。冻结 unit manifest、表示方式、向量校验摘要和修复代码；题目结构审查信息作为离线 sidecar，不能进入在线检索。
6. 使用主工作区本地凭据执行 120 次真实请求，严格保留失败记录。报告前校验冻结文件、执行键唯一性、完整性和调用次数。
7. 分别呈现旧 A→新 A 的共享收益、新 A→新 B3 的策略增益、B2 对照、分领域/结构适用子集、请求成功率、引用覆盖、P95、质量审查状态。离线重放不得当作新在线结果。

## 审查证据

原始审查：`docs/research/tree-rag/2026-09-23-b3-result-audit.md`。
原始结果目录：`py/.rag-evaluation/tree-b3-unit-40-v1`；修复前代码副本：主工作区 `.tmp/b3-repair-before`。
原始 dataset SHA256：`230bf4a271ba2bc2fc2d774357caf2bc2e21b46ae5eccf7c9e190b05a1d9eeb2`。
原始 dev-v5 SHA256：`fbc1ec03fcd2c185703b65f368393296322d7013188da43cbf6764f3d0699f27`。
