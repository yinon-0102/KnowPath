# 知识更新发现与采纳

延续已批准的 API 设计：预览只读；采纳仅接受已发布的完整绑定集合，使用 expected_space_version 与持久化幂等键。在空间锁和资料锁下，原子替换绑定、更新主题证据版本、标记受影响状态 stale、将旧计划持久化为 needs_replan，并保存完成 Run。

按内容和审核字段比较主题，忽略资料版本或来源 ID 单纯换代；未变化主题沿用旧证据版本，变化主题分配新版本，保留历史分数与来源。关系变动影响两端主题。未发布候选、回退和跨资料绑定均拒绝。显式学习范围不因节点消失而扩大。

先实现内存、SQLite、MySQL 的回归覆盖：预览不写入、重启/幂等、版本冲突、事务回滚、旧题防污染、计划失效；再接 HTTP 严格模型、完整回归、独立审查、模块提交推送。自动重排不伪造任务，调用现有 local_replan 创建新计划时返回独立 Run。

实现细节：在 binding JSON 中保存服务端 topic_revision_ids，未变化主题继承旧证据版本；变化主题分配新版本，原证据记录保持不可变。后台出题提交时校验启动绑定，发生变化则保留题目、状态 stale，并以 STALE_INPUT 结束 Run。计划 config 保存受影响主题，显式局部重排会保留旧任务历史并重新验证。

审查发现预览与评分的测验锁顺序相反，已通过真实 MySQL 持有测验行锁的测试复现 1205 锁等待。预览现改用无写锁的测验/计划读取；对应回归通过。已有完成任务在知识变更后的局部重排、include_review=false 下 stale 分数仍需重测，也均已增加先失败再修复的回归。

复核又识别生成结果落库的空间→测验锁顺序与消息发送相反，已用双事务真实 MySQL 回归复现 1213 死锁，并恢复为统一的测验→空间顺序，校验绑定和保存结果仍在同一事务。最终验证涵盖两种锁竞争边界。

最终全量 learning 回归：622 passed、30 skipped、1 warning（120.67 秒）。测试连接真实 MySQL、Neo4j、Qdrant，并使用确定性测试向量；未调用付费 DashScope。warning 为已有 Starlette/httpx 弃用提示。此次沿用现有 JSON 字段，无新增数据库迁移。
